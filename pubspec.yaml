name: ja_symlink
description: JA Symlink Manager - Windows Desktop App for managing directory symbolic links
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  logging: ^1.3.0
  path: ^1.9.0
  file_picker: ^8.0.0+1
  window_manager: ^0.4.3
  flutter_acrylic: ^1.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
